import React from "react";
import "./style.css";

function Header() {
    return (
        <header>
            <h1>Clicky Game</h1>
            <h2>Click on each image once - and only once!</h2>
        </header>
    )
}

export default Header;